INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src/tserver)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)

AUX_SOURCE_DIRECTORY(. SRC_DIR)
ADD_EXECUTABLE(tserver ${SRC_DIR})

TARGET_LINK_LIBRARIES(tserver pthread)

